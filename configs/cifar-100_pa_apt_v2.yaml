dataset: CIFAR100
first_split_size: 10
other_split_size: 10
schedule_type: cosine
optimizer: Adam
momentum: 0.9
weight_decay: 0
model_type: zoo
model_name: vit_pt_imnet
max_task: -1
dataroot: data
workers: 4
validation: False
train_aug: True
rand_split: True
# --- PA-APT V2 Settings ---
k_patches: 2          # Reduced number of patches for better stability
alpha_cls: 0.7        # Fusion weight for CLS token (high stability)
alpha_patch: 0.5      # Fusion weight for patch prompts (more plasticity)
use_layer_wise: True  # Enable layer-dependent patch count
